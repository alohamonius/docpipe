# --- secrets / deploy values — never commit ---
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
backend.hcl
.env
.env.*

# --- python ---
__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage.xml
.coverage

# --- generated by `make status` (regenerate, don't commit) ---
status.json
dashboard.html

# --- agent/run scratch: intermediate payloads, nothing durable ---
.scratch/

# --- misc ---
.DS_Store
*.zip
node_modules/

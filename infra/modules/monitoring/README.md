# module: monitoring (Phase 5)

CloudWatch dashboard (API latency, queue depth, Bedrock tokens, worker health),
alarms: DLQ depth > 0, Lambda error rate, Aurora capacity. SNS topic + email
subscription for alerts.

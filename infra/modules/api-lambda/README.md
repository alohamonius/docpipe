# module: api-lambda (Phase 3)

HTTP API Gateway (v2) with routes `POST /summarize`, `GET /jobs/{id}`,
Lambda function (Python 3.12, zip from CI), per-function log group with
retention, and route-level throttling.
